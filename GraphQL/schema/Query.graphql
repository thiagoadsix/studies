type Query {
    ola: String
    horaAtual: Date
    usuarioLogado: Usuario
    produtosEmDestaque: Produto
    numerosMegaSena: [Int!]!
    usuarios: [Usuario]
    usuario(id: Int): Usuario
    perfis: [Perfil]
    perfil(id: Int): Perfil
}
